import { accessToken, apiBaseUrl, movieBaseUrl } from "@/config";

export const MOVIE_BASE_URL = movieBaseUrl || "";
export const API_BASE_URL = apiBaseUrl || "";
export const ACCESS_TOKEN = accessToken || "";
export const API_ENDPOINT = {};
