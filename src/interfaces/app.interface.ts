export interface CarouselData {
  title: string;
  overview: string;
  backdrop_path: string;
  media_type: string;
  genres: string;
  redirect: string;
}

export interface GenreData {
  id: number;
  name: string;
}
